'use client'

// Reuse the existing Contracts UI for now.
// Layout title + nav selection are driven by pathname, so this route appears as a separate page.
export { default } from '../contracts/page'

