'use client'

/**
 * Same module as `/contracts` — layout, filters, and the desktop compact list (semantic `<table>`
 * + zebra striping) are shared. Pathname `/contract-performance` toggles Contract Performance
 * mode inside `contracts/page.tsx` (`isContractPerformance`).
 */
export { default } from '../contracts/page'

