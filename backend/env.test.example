NODE_ENV=test
JWT_SECRET=integration-test-jwt-secret-min-32-chars-long!!

DB_HOST=localhost
DB_PORT=5434
DB_NAME=klip_test
DB_USER=postgres
DB_PASSWORD=postgres

# Optional ClamAV: docker compose -f ../docker-compose.test.yml --profile clamav up -d
# CLAMD_HOST=127.0.0.1
# CLAMD_PORT=3310
